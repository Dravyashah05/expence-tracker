Add coin audio files here to use real transaction sounds:

- `coin-collect.mp3` for income
- `coin-drop.mp3` for expense

The app will use these files automatically from `/sounds/*`.
If files are missing, it falls back to generated tones.
